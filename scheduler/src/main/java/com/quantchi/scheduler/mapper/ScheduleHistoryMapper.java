package com.quantchi.scheduler.mapper;

import com.quantchi.scheduler.entity.ScheduleHistory;

import java.util.List;

public interface ScheduleHistoryMapper {

    Integer insert(ScheduleHistory history);

}
